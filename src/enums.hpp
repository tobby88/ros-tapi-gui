#ifndef ENUMS_H
#define ENUMS_H

enum class Device_Type {Input_Device, Output_Device, Hook};
enum class Device_Status_Response {OK, Error};
enum class Feature_Type {Camera, Button, Axis};

#endif // ENUMS_H
